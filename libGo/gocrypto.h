#ifndef GOXHAOS
#define GOXHAOS

long long int chaos(long long int key);
long long int xchaos(long long int key,char *X, int len);
long long int checksum(long long int key, char *X, int len);

#endif
